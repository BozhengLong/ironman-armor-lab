# 变更日志

按批次记录，最新在上。每条只写**做了什么、为什么、踩到什么**；
设计推导与实测数据在 `explode-design.md` / `pipeline.md`，此处不复述。

---

## R7 · 2026-08-29 · 泛化检验 · 模型清单 · Agent 工具层 · 移动端

`c575d34` `19b7c4f` `772bda2`

- **第三个模型（samurai）跑通，并推翻了一个参数。** 前两个模型上调出的
  `body_shell` 判据「跨度 > 55% 身高」在人形机甲上误判腿/臂/刀，污染高度参考系，
  导致 47% 的面被归进「头盔」。改为**同时要求贴底且贴顶**。前两个模型零回退。
- 新增 `accessory` 标记（`|lat| > 1.5`），处理立在旁边的刀这类非身体物件。
- **Agent 工具层** `window.armorLab`：10 个命令 + function-calling schema + 顺序执行器。
  页面自身不调用 LLM，只暴露命令面 —— 回到项目最初的命题。
- 模型清单改为数据驱动（`build_index.py` → `index.json`），前端不再写死模型名。
- 移动端：`touch-action`、触摸拖拽阈值 12px、820px 以下紧凑布局。
- WebGL 创建失败与上下文丢失都给可读提示，不再静默黑屏。

**踩到的**：samurai 的 1024 贴图预算原本靠调用时手传 `--tex`，CI 用默认 2048 会产出
17.56MB 而非 7.08MB —— **本地与 CI 分叉**。已把预算写进 `TEX_BUDGET` 配置。

---

## R6 · 2026-08-28 · 组内逐件浏览；连通分量拆分否决

`9c83e15`

- 实测后**放弃**按连通分量拆分：源 mesh 确实没焊死（可程序拆分，不需要 Blender），
  但任何阈值下拆完都是 582–1295 个零件。依据见 `explode-design.md`。
- 改用交互解决同一诉求：选中组后 ← → 逐件浏览，三档不透明度。
- 新增 `backup_assets.py`：打包后解到临时目录按锁文件验证还原。

---

## R5 · 2026-08-28 · ASSEMBLE 组装动画

`a086076`

按穿戴顺序逐组归位（脚→腿→躯干→臂→头盔），收尾点亮发光件。
位置由逐零件时间轴驱动，相机取景由全局进度驱动，两者解耦。

**踩到的**：组内错峰幅度一度大于组间间隔，导致肩甲先于它所覆盖的胸甲到位 ——
回归测试抓到的。已改为受组间间隔约束。

---

## R4 · 2026-08-28 · 仓库转公开 + Pages 上线

`242341e` `2e73d01`

- **为什么从 private 转 public**：GitHub Pages 在免费计划的私有仓库上不可用；
  更关键的是**仓库可见性保护不了模型** —— 一旦部署到任何公开站点，
  GLB 就是公网可直接下载的。所以「private 仓库 + 公开 Pages」自相矛盾。
  评估过 Cloudflare Pages（免费、可保持仓库私有）与 Cloudflare Access
  （唯一能真正不公开资源的方案），最终选了最简单的公开路径。
- **公开前重写了 git 历史**，移出两份文件：一份是个人对话记录，
  一份含对具体上传者的来源推断（点名的公开指控）。二者仅留本地，已入 `.gitignore`。
  只从 HEAD 移除不够 —— 内容仍在历史里。这是**唯一干净的时机**（当时无 fork 无 star）。
- 提交者邮箱按用户选择保留为真实地址。

**踩到的**：`--force-with-lease` 报 stale info —— `filter-branch` 连本地的
`origin/main` 引用一起重写了，租约期望值失效。需先 `git fetch` 同步真实远端状态。

---

## R3 · 2026-08-28 · CI 构建与部署

`c91523c` `cdf55a1` `d0ad25c`

- `push` 只构建校验，**发布必须手动触发**（`workflow_dispatch` + `inputs.deploy`）。
  原本是防止意外公开；公开后意义变成「控制何时上线」。
- CI 按 `assets.lock.json` 哈希缓存资源，并校验 manifest 可由源资源逐字节复现。
- 页面全部改用相对路径，适配 Pages 项目站点的 `/<repo>/` 子路径。
- three 依赖改为解析 ES 导入图递归收集 —— 手写清单漏过 `build/three.core.js`，页面白屏。

**踩到的**：CI 首次全新下载暴露 `ATTRIBUTION.txt` 由脚本自己生成，
改过文案后本地 `--relock` 锁的是旧文案哈希 → 校验失败。**锁文件只应锁上游内容。**

---

## R2 · 2026-08-28 · 钻取交互 + 压缩管线

`728ae1d`

- 钻取：点选子装配体 → 其余幽灵化 → 组内展开 → 相机对焦 → 面板显示详情。
- 压缩：49MB → 4.5MB。贴图是主要杠杆，不是几何。
- 因 manifest 以 glTF node 索引为键，管线带**结构指纹校验**，节点被重排即拒绝产出。

**踩到的**（细节见 `explode-design.md`）：材质在节点间共享、双面透明材质渲染两遍、
取景用 `max(宽,深)` 低估斜视角、拾取 5.7ms 过贵。

---

## R1 · 2026-08-27 ~ 28 · 调研 → 分类器 → 爆炸规划 → 仓库

`870470f`

- 用 Sketchfab Viewer API 的 `getNodeMap` **远程探测 20 个模型的场景树**，
  不下载即可判断能否做拆解交互。
- 部件分类器（四层机制）+ 爆炸规划（发现径向爆炸对嵌套壳层根本无效，
  改按语义组刚体分离）+ 最小验证页。
- 仓库初始化。**Git LFS 建过又拆掉**：`assets/raw/` 是下载缓存不是源码，
  给缓存做版本控制在原理上就错，与体积无关。改为提交校验和锁文件。
  `.gitattributes` 里保留了注释掉的 LFS 规则与 Pages 不解析指针的告诫。
